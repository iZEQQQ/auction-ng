import { GetCategoryResponse } from './get-category-response';

describe('GetCategoryResponse', () => {
  it('should create an instance', () => {
    expect(new GetCategoryResponse()).toBeTruthy();
  });
});
