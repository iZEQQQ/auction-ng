import { GetCategoriesResponse } from './get-categories-response';

describe('GetCategoriesResponse', () => {
  it('should create an instance', () => {
    expect(new GetCategoriesResponse()).toBeTruthy();
  });
});
