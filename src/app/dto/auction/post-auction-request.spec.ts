import { PostAuctionRequest } from './post-auction-request';

describe('PostAuctionRequest', () => {
  it('should create an instance', () => {
    expect(new PostAuctionRequest()).toBeTruthy();
  });
});
