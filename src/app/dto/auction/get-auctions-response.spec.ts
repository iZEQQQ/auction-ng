import { GetAuctionsResponse } from './get-auctions-response';

describe('GetAuctionsResponse', () => {
  it('should create an instance', () => {
    expect(new GetAuctionsResponse()).toBeTruthy();
  });
});
